{
  "name": "dots-live",
  "dependencies": {
    "next": "^4.2.1",
    "react": "^16.0.0",
    "react-dom": "^16.0.0"
  },
  "scripts": {
    "dev": "next",
    "build": "next build",
    "export": "next export",
    "start": "next start",
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^4.3.0",
    "prettier": "^1.7.4"
  },
  "lint-staged": {
    "linters": {
      "*.js": [
        "prettier --no-semi --single-quote --print-width 100 --trailing-comma es5 --write",
        "git add"
      ]
    }
  }
}
